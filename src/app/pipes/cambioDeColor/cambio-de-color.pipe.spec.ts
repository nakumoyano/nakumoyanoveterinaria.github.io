import { CambioDeColorPipe } from './cambio-de-color.pipe';

describe('CambioDeColorPipe', () => {
  it('create an instance', () => {
    const pipe = new CambioDeColorPipe();
    expect(pipe).toBeTruthy();
  });
});
